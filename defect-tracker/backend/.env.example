PORT=3002

# Defect tracker database
PGHOST=localhost
PGPORT=5432
PGDATABASE=linxas_defects
PGUSER=defects_user
PGPASSWORD=

# Portal database (user lookups — read-only access)
PORTAL_PGDATABASE=linxas_portal
PORTAL_PGUSER=portal_user
PORTAL_PGPASSWORD=Portal@1234

# Auth — must match the portal's JWT_SECRET exactly
JWT_SECRET=
JWT_EXPIRES_IN=7d

CORS_ORIGIN=http://10.0.5.125
